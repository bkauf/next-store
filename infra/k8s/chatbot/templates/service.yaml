apiVersion: v1
kind: Service
metadata:
  name: chatbot
  namespace: chatbot
spec:
  type: LoadBalancer
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http-web-svc
      protocol: TCP
      name: http
  selector:
    app: chatbot
