# Default values for chatbot.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  pullPolicy: Always
  tag: "latest"

imagePullSecrets: []

service:
  port: 80
  targetPort: 3000

weaviate:
  server: weaviate.weaviate.pod.cluster.local


